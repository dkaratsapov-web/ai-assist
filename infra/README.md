# Развёртывание стенда в Яндекс Облаке

Пошаговая инструкция для домена **ai-helper.pro**. От начала до работающего
стенда — около часа, из которых большая часть уходит на сборку образов.

> **Это staging-стенд, а не production.** Аутентификация продукта ещё не
> реализована: контекст организации приходит заголовками, и backend намеренно
> отказывается так работать при `APP_ENV=production`. Поэтому стенд поднимается
> с `APP_ENV=staging` и закрыт паролем на прокси. Свойство «в production так
> нельзя» при этом сохраняется — оно проверяется кодом, а не дисциплиной.

---

## 1. Виртуальная машина

В консоли Яндекс Облака: **Compute Cloud → Создать ВМ**.

| Параметр        | Значение                                                             |
| --------------- | -------------------------------------------------------------------- |
| Образ           | Ubuntu 24.04 LTS                                                     |
| Платформа       | Intel Ice Lake                                                       |
| vCPU / RAM      | 2 vCPU (100% vCPU), 4 ГБ                                             |
| Диск            | 30 ГБ SSD                                                            |
| Публичный адрес | Статический — иначе адрес сменится при перезапуске и домен отвалится |
| SSH-ключ        | Ваш публичный ключ                                                   |

Ориентировочно 1000–1500 ₽ в месяц. Для стенда этого достаточно: тяжёлые
процессы (краулер, сбор семантики) появятся позже и потребуют пересмотра.

**Группа безопасности** — входящие правила только для 22, 80 и 443. Порты
PostgreSQL и Redis наружу не открываются: база не должна быть доступна из
интернета (v0.3 §104), и compose их не публикует.

## 2. Домен

В DNS для `ai-helper.pro` создать A-запись на публичный адрес ВМ. Отдельная
запись для API не нужна: и сайт, и API живут на одном домене, API — по пути
`/api`. Так исчезает CORS и не приходится выпускать второй сертификат.

Дождитесь, пока имя начнёт резолвиться, — Caddy получает сертификат по домену,
и до появления DNS-записи это не сработает.

## 3. Подготовка сервера

```bash
ssh ubuntu@<адрес-вм>

sudo apt-get update && sudo apt-get install -y ca-certificates curl git
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo tee /etc/apt/keyrings/docker.asc > /dev/null
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo usermod -aG docker $USER && newgrp docker
```

## 4. Код и конфигурация

```bash
git clone https://github.com/dkaratsapov-web/ai-assist.git
cd ai-assist/infra
cp .env.example .env
```

Заполнить `.env`:

```bash
# Пароль для входа на стенд
docker run --rm caddy:2-alpine caddy hash-password --plaintext 'придумайте-пароль'
# полученный хеш → BASIC_AUTH_HASH

# Пароль базы
openssl rand -base64 24
# → POSTGRES_PASSWORD
```

`NEXT_PUBLIC_DEMO_ORG_ID` и `NEXT_PUBLIC_DEMO_USER_ID` пока оставьте пустыми —
их выдаст скрипт демо-данных на следующем шаге.

## 5. Первый запуск

```bash
docker compose --env-file .env up -d --build
```

Сборка занимает 5–15 минут. Дальше — демонстрационные данные:

```bash
docker compose exec api python scripts/seed_demo.py
```

Скрипт напечатает идентификаторы организации и пользователя. Впишите их в
`.env` и пересоберите фронтенд — эти значения попадают в браузерный бандл на
этапе сборки:

```bash
docker compose --env-file .env up -d --build web
```

Готово: `https://ai-helper.pro` спросит логин и пароль, дальше откроется
интерфейс.

## 6. Проверка

```bash
docker compose ps                      # все сервисы healthy
docker compose logs -f api             # структурированные логи
curl -u team:пароль https://ai-helper.pro/api/v1/health
```

Ответ `health` показывает режим работы: `ai_provider: stub` и
`ad_platform_adapter: mock` означают, что стенд работает на заглушках и
результаты не являются выводами настоящей модели.

## 7. Обновление

```bash
git pull
docker compose --env-file .env up -d --build
```

Миграции применяются отдельным сервисом до старта API: он не поднимется, пока
схема не обновлена.

---

## Что осознанно не сделано

Это стенд, а не боевая инфраструктура. Ниже — то, что появится к MVP 2, когда
подключатся реальные рекламные кабинеты, и почему пока не нужно.

**Резервные копии.** Данных, которые жалко потерять, на стенде нет: всё
демонстрационное и восстанавливается скриптом. Требования по RPO/RTO из
v0.4 §14 относятся к боевому контуру.

**Раздельные окружения dev/staging/prod** (v0.3 §108). Пока контур один — их
незачем разделять. К моменту первого production-токена это обязательно.

**Секреты в Secret Manager.** Сейчас они в `.env` на сервере. Для стенда без
реальных токенов это приемлемо; как только появится OAuth-токен рекламного
кабинета — обязателен Yandex Lockbox или KMS, потому что токен даёт доступ к
чужим бюджетам (v0.3 §94).

**Сборка образов в CI и реестр.** Сейчас образы собираются на самой машине.
Это проще и не требует настройки Container Registry, но занимает ресурсы ВМ во
время сборки. При переходе на несколько окружений — вынести в CI.

**Отдельный воркер для краулера с сетевой изоляцией** (v0.4 §20). Появится
вместе с самим краулером в следующем срезе: краулер обязан работать в отдельном
контейнере с собственными правилами исходящего трафика, и добавлять его в этот
compose нужно сразу с ними.
